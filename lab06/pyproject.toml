[project]
name = "lab06"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nano-trm",
    "nbconvert>=7.16.6",
]

[tool.uv.sources]
nano-trm = { git = "https://github.com/allthingssecurity/trm_sudoku.git" }
